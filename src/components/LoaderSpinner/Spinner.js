import { Box } from "@mui/material";
import React from "react";
import styled from "./Spinner.module.css";
const Spinner = () => {
  return <Box className={styled.loader}></Box>;
};

export default Spinner;
