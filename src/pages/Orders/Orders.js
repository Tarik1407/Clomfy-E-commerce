import React from "react";
import { Box, Typography } from "@mui/material";

const Orders = () => {
  return (
    <Box>
      <Typography variant="p1">Orders Page</Typography>
    </Box>
  );
};

export default Orders;
